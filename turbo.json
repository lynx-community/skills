{
  "$schema": "https://turborepo.dev/schema.json",
  "globalPassThroughEnv": ["GIT_SSH_COMMAND", "XDG_CONFIG_HOME"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
