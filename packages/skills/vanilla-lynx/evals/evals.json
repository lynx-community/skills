{
  "skill_name": "vanilla-lynx",
  "description": "Outcome-based evals for build-free Vanilla Lynx XML (`.lynxml`) authoring, Element PAPI rendering, threading, styling, and external bundles. Equivalent correct implementations and explanations should pass.",
  "evals": [
    {
      "id": 1,
      "name": "author-build-free-lynxml-counter",
      "prompt": "请把一个简单的 Vanilla Lynx 计数器写成 Lynx 可以直接消费的完整 .lynxml 文件：包含样式和 main-thread Element PAPI 逻辑，不经过 Rspeedy 或其他构建步骤，并说明什么时候才需要 background source block。",
      "expected_output": "A complete build-free `.lynxml` document with the Lynx envelope, style and main-thread source blocks, a local Element PAPI counter interaction, correct lifecycle cleanup, and no app build configuration. The background block remains optional and is reserved for heavier work.",
      "expectations": [
        "The answer produces one complete `.lynxml` document beginning with `<!doctype lynx>` and containing one lowercase `lynx` root with a non-empty quoted `engine-version`.",
        "The document places CSS in a direct-child `style` block and Element PAPI code in a direct-child `<script thread=\"main\">` block, with all source blocks and the root closed.",
        "The main script creates Page, View, Text, and RawText nodes with Element PAPI; each RawText is a direct child of Text and later text updates retain the Text parent.",
        "The local counter interaction uses the runtime event name `tap`, supplies all four listener arguments, and removes the same listener tuple during cleanup.",
        "The initial render relies on the SDK flush path, while the later counter mutation calls `__FlushElementTree()` after its final UI change.",
        "The answer does not introduce ReactLynx, JSX, an XML declaration, CDATA, a `config` block, `package.json`, `lynx.config.ts`, Rspeedy, or `@lynx-js/vanilla-rsbuild-plugin`.",
        "The answer treats `<script thread=\"background\">` as optional and adds it only for heavier business logic, async work, timers, native calls, or data processing; background code never mutates Element PAPI UI."
      ],
      "files": []
    },
    {
      "id": 2,
      "name": "lynxml-main-background-patch",
      "prompt": "请写一个完整 .lynxml：main script 创建 page/view/text，按钮点击后把一个明确的重计算任务发给 background script，收到 serializable patch 后更新文本并 flush。不要 React 或 Rspeedy。",
      "expected_output": "A complete `.lynxml` artifact whose main script creates and updates an Element PAPI tree, delegates genuinely heavier work to an optional background script, exchanges serializable messages through the correct contexts, flushes later UI mutations, and cleans up both sides.",
      "expectations": [
        "Engine-defined lifecycle events are registered through `lynx.getEngine()`. Handlers may accept or ignore event payloads, and an update hook may be a placeholder when engine-driven updates are unused.",
        "The initial UI tree and later UI mutations use appropriate Element PAPI APIs on the main thread.",
        "Every `__AddEventListener` call supplies its four required arguments, including an options object such as `{}` when no options are needed.",
        "Main-to-background tasks and background-to-main patches use the appropriate runtime bridge. Variable names and helper structure are not prescribed.",
        "Every app-defined Runtime Context dispatch uses an object containing both `type` and `data`, including the app-defined background teardown message.",
        "The initial tree is created with Element PAPI on the main thread. Omitting `__FlushElementTree()` from the initial render path is sufficient evidence of SDK-managed initial flushing and does not require an accompanying explanation. Later UI mutations are followed by `__FlushElementTree()`.",
        "Long-lived runtime or node listeners are cleaned up during teardown."
      ],
      "files": []
    },
    {
      "id": 3,
      "name": "background-thread-state-and-patches",
      "prompt": "在 build-free .lynxml 中，background script 应该怎样接收 main script 消息、维护后台状态、执行异步或重计算任务、避免首屏数据被立即回写，再把 serializable patch 发回 main？请同时说明清理和 UI 线程边界。",
      "expected_output": "An explanation or code sketch that keeps background state and heavier work off the UI thread, communicates through the correct runtime context, sends serializable patches to the main thread, avoids unnecessary first-screen echoing, and cleans up listeners.",
      "expectations": [
        "The cross-thread endpoints are complementary: background code uses `lynx.getCoreContext()` to communicate with the main thread, while main-thread code may use `lynx.getJSContext()` to communicate with the background. Showing both in their respective scripts is correct, not contradictory; helper and event names may vary.",
        "Background-owned state and heavier async, timer, native, data-processing, or business work remain on the background thread.",
        "The design avoids an unnecessary first-screen echo unless a second update is intentional, and sends small serializable changes rather than functions or node handles.",
        "Runtime Context messages use `{ type, data }`; receivers read `event.data`, and teardown uses an app-defined event rather than dispatching through `lynx.getEngine()`.",
        "The background thread does not call Element PAPI APIs or `__FlushElementTree()`; the main thread owns UI mutation and flushing.",
        "Background listeners are removed through a real teardown path; the exact cleanup mechanism may vary."
      ],
      "files": []
    },
    {
      "id": 4,
      "name": "style-vanilla-lynx",
      "prompt": "我要给 build-free .lynxml 的 style block 写样式。请说明 Lynx 在布局、盒模型、文本换行、响应式、选择器、定位和动态 transform 方面需要遵守的规则，并说明 main script 怎样在运行时把 class 应用到节点上。",
      "expected_output": "Accurate Vanilla Lynx styling guidance covering runtime class application and the requested layout, box-model, text, responsive, selector, and positioning differences. Equivalent explanations and valid responsive techniques are acceptable.",
      "expectations": [
        "Runtime styling uses Element PAPI class APIs such as `__SetClasses()` or `__AddClass()`; inline styles are reserved primarily for computed values.",
        "The response accurately covers Lynx layout and box-model behavior, including the default column-oriented layout and the lack of margin collapsing.",
        "Text guidance accounts for text nodes and explicit wrapping when wrapping is required.",
        "The response notes that CSS `@media` is unavailable and gives at least one viable responsive approach, such as a viewport-based root size with `rem`, direct viewport units, or JavaScript-driven classes.",
        "Selector and positioning guidance reflects Lynx limitations, including unreliable structural pseudo selectors or pseudo-elements and platform-specific position or `z-index` rules. Exact wording is not required.",
        "A node whose transform changes later has a non-zero layout box and an initial transform before the first flush; responsive interaction geometry does not assume one hard-coded device center."
      ],
      "files": []
    },
    {
      "id": 5,
      "name": "thread-local-and-cross-thread-events",
      "prompt": "build-free .lynxml 的 runtime context 怎么区分线程内事件闭环和双线程通信？请分别给出 main、background source block 的本地事件，以及 main ↔ background 的 context 选择规则。",
      "expected_output": "A correct explanation, table, or code example distinguishing thread-local events from both directions of cross-thread communication, with appropriate contexts, cleanup, and serializable payloads.",
      "expectations": [
        "The response correctly distinguishes local contexts: main-thread `lynx.getCoreContext()` and background-thread `lynx.getJSContext()` stay within their calling threads.",
        "Both cross-thread directions use the complementary pair: main-thread `lynx.getJSContext()` and background-thread `lynx.getCoreContext()`. Showing both getters in their respective scripts is expected and must not be treated as inconsistent. A table, prose explanation, or code example can demonstrate this.",
        "Contexts are used consistently for dispatch and listener management; exact variable names or helper shapes are irrelevant.",
        "Every dispatch uses `{ type, data }`, receivers read `event.data`, and a no-payload dispatch still includes `data`.",
        "`lynx.getEngine()` is reserved for engine lifecycle events rather than app-defined messaging.",
        "Long-lived listeners use removable handler references and are cleaned up through teardown; forwarding destroy is one valid approach, not the only accepted form.",
        "Cross-thread payloads are serializable and do not carry functions or Element PAPI node handles."
      ],
      "files": []
    },
    {
      "id": 6,
      "name": "background-external-bundle-build-and-load",
      "prompt": "我想给 build-free .lynxml 配一个独立的纯 TypeScript background-thread .lynx.bundle：用 rslib 构建，再从 background script 通过 lynx.fetchBundle 和 lynx.loadScript 加载。如果结果会影响 UI，完整流程应该怎么写？",
      "expected_output": "A correct background-only external-bundle build and loading flow for plain TypeScript or JavaScript, including the Rslib layer configuration, fetch/load relationship, asynchronous export use, and main-thread UI handoff. Equivalent code organization is acceptable.",
      "expectations": [
        "The external module is plain TypeScript or JavaScript and does not depend on ReactLynx, JSX, or ReactLynx transforms.",
        "The Rslib setup uses `defineExternalBundleRslibConfig` or an equivalent supported configuration and targets the background layer, including the required layer exposure where applicable.",
        "The source entry key and the first `lynx.loadScript` argument refer to the same exported entry. Examples and exact wording are optional.",
        "Fetching and loading happen on the background thread with an absolute bundle URL that the target device can reach and that returns bundle bytes; placeholder domains are not presented as runnable defaults. Success/error handling is present, and the fetched bundle URL is supplied to `loadScript` as needed.",
        "Loaded exports are used after asynchronous loading completes rather than before they are available.",
        "Results that affect UI are sent as serializable data to the main thread, which performs Element PAPI mutation and flushing."
      ],
      "files": []
    },
    {
      "id": 7,
      "name": "interactive-image-card",
      "prompt": "请写一个完整 build-free .lynxml 图片选项卡：点击 Element PAPI 按钮时切换现有 Image 的 src，用 dataset 标识当前选项，Image 上方有不应拦截点击的装饰层。不要 ReactLynx、JSX 或构建配置。",
      "expected_output": "A complete `.lynxml` interaction that uses the runtime tap event, valid dataset calls, a passive overlay, flicker-free image source changes, later main-thread flushing, and matching cleanup.",
      "expectations": [
        "The answer is one complete `.lynxml` document with the canonical root and closed style, main-script, and root blocks; every referenced lifecycle or node-event handler is declared.",
        "The button listener uses the runtime event name `tap`, not markup binding names such as `bindtap` or `catchtap`, and supplies all four Element PAPI listener arguments.",
        "Listener cleanup reuses the corresponding node, event name, handler, and options.",
        "Dataset writes use `__SetDataset(element, datasetObject)` or `__AddDataset(element, key, value)` rather than a key/value form of `__SetDataset`.",
        "The Image receives `defer-src-invalidation` before its initial `src`, and later logic avoids assigning the same source redundantly.",
        "The passive overlay does not intercept the button. Lynx CSS `pointer-events: none` is the expected current solution; an equivalent hit-testing mechanism supported by the explicitly targeted Lynx version is acceptable. `user-interaction-enabled` is not emitted as a CSS property.",
        "The later image-source mutation stays on the main thread and is followed by `__FlushElementTree()`."
      ],
      "files": []
    }
  ]
}
