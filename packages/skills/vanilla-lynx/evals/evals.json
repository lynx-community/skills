{
  "skill_name": "vanilla-lynx",
  "description": "End-to-end task evals for building Lynx apps directly with Vanilla Lynx Element PAPI APIs, Rspeedy native artifacts, Lynx CSS constraints, and main/background runtime event communication.",
  "evals": [
    {
      "id": 1,
      "name": "scaffold-rspeedy-vanilla-lynx-project",
      "prompt": "我要做一个不用 ReactLynx/JSX 的 Lynx 卡片，直接用 Element PAPI 构造 Vanilla Lynx 页面。请按当前 skill 给出可被 Rspeedy 构建成原生 .bundle 的最小项目结构，并说明每个文件负责什么。",
      "expected_output": "A minimal Vanilla Lynx project containing `package.json`, `lynx.config.ts`, `src/main-thread.ts`, optional `src/background.ts`, `src/style.css`, and `src/rspeedy-env.d.ts`. The project uses Rspeedy with `@lynx-js/vanilla-rsbuild-plugin`; `pluginVanillaLynx()` receives the main-thread entry and discovers the sibling background and CSS sources.",
      "expectations": [
        "The proposed project uses Element PAPI directly without introducing ReactLynx, JSX, virtual DOM libraries, or browser DOM APIs.",
        "The layout includes `package.json` with Rspeedy build or dev scripts and dependencies on `@lynx-js/rspeedy` and `@lynx-js/vanilla-rsbuild-plugin`.",
        "The layout includes `lynx.config.ts`, imports `defineConfig` and `pluginVanillaLynx`, points `source.entry` at `main-thread.ts`, and registers `pluginVanillaLynx()`.",
        "The layout includes `src/main-thread.ts`, optional sibling `src/background.ts`, `src/style.css`, and `src/rspeedy-env.d.ts`, and says the plugin discovers the sibling background and CSS sources automatically.",
        "The answer says the main thread owns Element PAPI tree creation and UI updates, while the optional background thread owns heavier logic and returns serializable data for UI updates."
      ],
      "files": []
    },
    {
      "id": 2,
      "name": "main-thread-counter-tree",
      "prompt": "给我一个 Vanilla Lynx main-thread 计数器骨架：主线程创建 page/view/text，监听 engine 生命周期，点击后把任务发给 background，收到 background patch 后更新文本并 flush。只要关键代码结构，不要 React 代码。",
      "expected_output": "A main-thread entry that uses `lynx.getEngine()` for `__RenderPage`, `__UpdatePage`, and `__DestroyLifetime`, uses Element PAPI APIs on the main thread to create and mutate the tree, stores `lynx.getJSContext()` as `backgroundThread` and reuses it to dispatch events to the background thread and add or remove background-patch listeners, flushes only after later updates, and removes runtime listeners during destroy.",
      "expectations": [
        "The code uses `lynx.getEngine()` and the engine-defined lifecycle event names `__RenderPage`, `__UpdatePage`, and `__DestroyLifetime` exactly.",
        "The code creates the UI tree on the main thread with Element PAPI APIs such as `__CreatePage`, `__GetElementUniqueID`, `__CreateView`, `__CreateText` or `__CreateRawText`, and `__AppendElement`.",
        "The code explicitly uses `const backgroundThread = lynx.getJSContext()` to access the background-thread environment and dispatch a UI task or data event to it.",
        "The main-thread code reuses the same context returned by `lynx.getJSContext()` to dispatch events and to add or remove the listener for background patches.",
        "The code receives a background patch, mutates existing text or child nodes on the main thread, and calls `__FlushElementTree()` after that later mutation.",
        "The code does not call `__FlushElementTree()` during initial `renderPage`, and it removes engine or background-bridge listeners when `__DestroyLifetime` runs."
      ],
      "files": []
    },
    {
      "id": 3,
      "name": "background-thread-state-and-patches",
      "prompt": "Vanilla Lynx 的后台线程应该怎样接收主线程消息、维护后台状态、执行异步或重计算任务、避免首屏数据被立即回写，再把 serializable patch 发给主线程？请同时说明清理和 UI 线程边界。",
      "expected_output": "An explanation that the background thread reuses `lynx.getCoreContext()` to receive main-thread messages and dispatch serializable patches, owns background state and heavier work, suppresses an unnecessary first-screen echo, never calls Element PAPI or flush APIs, and removes listeners during destroy.",
      "expectations": [
        "The response reuses one context returned by `lynx.getCoreContext()` in the background thread for `addEventListener`, `dispatchEvent`, and `removeEventListener`.",
        "The response keeps background-owned state separate and avoids echoing the first-screen data back to the main thread unless a second update is intentionally required.",
        "The response keeps heavier async, timer, native, data-processing, or business logic on the background thread.",
        "The response sends only changed, small, serializable fields to the main thread in `PatchFromBackground` or an equivalent event.",
        "The response says the background thread must not call Element PAPI APIs or `__FlushElementTree()` and that the main thread owns the resulting UI mutation.",
        "The response removes background listeners when the destroy lifecycle arrives."
      ],
      "files": []
    },
    {
      "id": 4,
      "name": "style-vanilla-lynx",
      "prompt": "我要给 Vanilla Lynx 页面写样式。请说明 Lynx 在布局、盒模型、文本换行、响应式、选择器和定位方面需要遵守的规则，并说明主线程怎样在运行时把 class 应用到节点上。",
      "expected_output": "Guidance based on the style reference that applies classes with Element PAPI at runtime, uses explicit Lynx layouts, and follows Lynx-specific runtime style constraints.",
      "expectations": [
        "The response applies classes from the main thread at runtime with `__SetClasses()` or `__AddClass()` and reserves `__SetInlineStyles()` for runtime-computed values.",
        "The response explains that Lynx defaults to column Linear layout and should use explicit Linear, Flex, Grid, or Relative layout as appropriate.",
        "The response says Lynx defaults to `box-sizing: auto`, which usually behaves like `border-box`, and does not collapse margins.",
        "The response says text must use text nodes, does not wrap by default, and should use `white-space: normal` when wrapping is required.",
        "The response says `@media` is unsupported, explicitly recommends `rem` + `vw` by setting a viewport-based root font size and using `rem` for component dimensions, and mentions direct viewport units or JavaScript as alternatives.",
        "The response warns that structural pseudo-classes and `::before` or `::after` can parse without taking effect, and recommends explicit classes or real nodes.",
        "The response says `position` defaults to `relative`, `static` is unsupported, and `z-index` must be paired with `position`."
      ],
      "files": []
    },
    {
      "id": 5,
      "name": "thread-local-and-cross-thread-events",
      "prompt": "Vanilla Lynx 的 runtime context 怎么区分线程内事件闭环和双线程通信？请分别给出 main-thread.ts、background.ts 的本地事件，以及 main ↔ background 的 context 选择规则。",
      "expected_output": "An explanation that a main-thread local event reuses `lynx.getCoreContext()` for dispatching and listener management, a background-thread local event reuses `lynx.getJSContext()`, and cross-thread communication pairs main-thread `lynx.getJSContext()` with background-thread `lynx.getCoreContext()`. Long-lived cross-thread listeners retain stable handler references and are removed during cleanup.",
      "expectations": [
        "The response says a main-thread local event uses the same context returned by `lynx.getCoreContext()` for `dispatchEvent`, `addEventListener`, and `removeEventListener`.",
        "The response says a background-thread local event uses the same context returned by `lynx.getJSContext()` for `dispatchEvent`, `addEventListener`, and `removeEventListener`.",
        "The response says main-thread `lynx.getCoreContext()` is a self-loop and does not deliver the event to background.",
        "The response preserves the cross-thread pairing: main-thread `lynx.getJSContext()` dispatch is received by a listener managed through background-thread `lynx.getCoreContext()`, and the reverse direction uses the same two endpoints.",
        "Cross-thread listeners use stable named handler references instead of inline callbacks and are removed during cleanup with the same context and event name used for registration."
      ],
      "files": []
    },
    {
      "id": 6,
      "name": "background-external-bundle-build-and-load",
      "prompt": "我想在 Vanilla Lynx app 中把纯 TypeScript 工具库用 rslib 打成独立的 background-thread .lynx.bundle，再用 lynx.fetchBundle 和 lynx.loadScript 加载。如果调用结果会影响 UI，完整流程应该怎么写？",
      "expected_output": "A background-only `defineExternalBundleRslibConfig` config plus background-thread loading code that fetches the `.lynx.bundle` by absolute URL, checks `response.code === 0`, calls `lynx.loadScript` with the rslib entry key and `response.url` as `bundleName`, uses the loaded exports asynchronously, and sends serializable results to the main thread for UI updates.",
      "expectations": [
        "The rslib config uses `defineExternalBundleRslibConfig`, exposes `LAYERS`, pins the entry to `LAYERS.BACKGROUND`, and does not add ReactLynx transforms for plain TypeScript or JavaScript.",
        "The answer states that the first `loadScript` argument is the rslib entry key and that `id` determines the emitted `<id>.lynx.bundle` filename.",
        "The answer keeps loading on the background thread and does not instruct the user to load an external bundle from the main thread.",
        "The runtime code uses an absolute URL, checks `response.code === 0`, calls `lynx.loadScript` inside the `fetchBundle` callback, passes `response.url` as `bundleName`, and wraps evaluation in `try`/`catch`.",
        "The answer uses the loaded exports inside the asynchronous callback.",
        "When the external method result affects UI, the answer sends a serializable result to the main thread and leaves Element PAPI mutation to the main thread."
      ],
      "files": []
    }
  ]
}
