---
"@lynx-js/skill-vanilla-lynx": minor
---

Add external bundle guidance: build a package into a standalone `.lynx.bundle` with rslib, then load it at runtime with `lynx.fetchBundle` and `lynx.loadScript`.
