---
"@lynx-js/skill-vanilla-lynx": minor
---

Add external bundle guidance, split into a build reference (rslib) and a runtime reference (`lynx.fetchBundle` / `lynx.loadScript`), scoped to background-thread use.
