---
"@lynx-js/skill-vanilla-lynx": patch
---

Document that `__AddEventListener(element, eventName, handler, options)` requires all four arguments, uses `{}` when no options are needed, and must be paired with matching listener cleanup.
